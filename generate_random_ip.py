import random

import random
import colorsys
import colored
import colorama
import banner
import webbrowser
from pystyle import Colorate, Colors
import os
import pystyle
import subprocess
import socket

COLOR_CODE = {
        "GREEN": "\033[32m",
        "BOLD": "\033[01m",
        "RESET": "\033[0m",
    }

import random

def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))


def generate_random_ip():
     return '.'.join(str(random.randint(0, 255)) for _ in range(4))

ip_address = generate_random_ip()
print(Colorate.Horizontal(Colors.red_to_yellow, (ip_address).strip()))
  
 